server:
  port: 8081

spring:
  application:
    name: Entry

  datasource:
    url: jdbc:h2:file:./entrydb
    username: sa
    password: 
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
        